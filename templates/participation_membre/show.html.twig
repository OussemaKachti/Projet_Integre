{% extends 'base.html.twig' %}

{% block title %}ParticipationMembre{% endblock %}

{% block body %}
    <h1>ParticipationMembre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participation_membre.id }}</td>
            </tr>
            <tr>
                <th>DateRequest</th>
                <td>{{ participation_membre.dateRequest ? participation_membre.dateRequest|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ participation_membre.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_participation_membre_index') }}">back to list</a>

    <a href="{{ path('app_participation_membre_edit', {'id': participation_membre.id}) }}">edit</a>

    {{ include('participation_membre/_delete_form.html.twig') }}
{% endblock %}
