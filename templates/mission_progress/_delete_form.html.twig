<form method="post" action="{{ path('app_mission_progress_delete', {'id': mission_progress.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission_progress.id) }}">
    <button class="btn">Delete</button>
</form>
