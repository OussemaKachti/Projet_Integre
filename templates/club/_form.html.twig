{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate', 'id': 'clubForm'}}) }}

<ol class="list-unstyled">
    <li class="mb-3">
        {{ form_label(form.nomC, 'Club Name', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.nomC, {'attr': {'class': 'form-control', 'placeholder': 'Enter club name', 'required': 'required'}}) }}
        {{ form_errors(form.nomC) }}
    </li>

    <li class="mb-3">
        {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Enter description', 'required': 'required'}}) }}
        {{ form_errors(form.description) }}
    </li>

    <li class="mb-3">
        {{ form_label(form.image, 'Club Image', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.image, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
        {{ form_errors(form.image) }}
    </li>

    <li class="mb-3">
        {{ form_label(form.president, 'President', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.president, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
        {{ form_errors(form.president) }}
    </li>
</ol>

<button type="submit" class="btn btn-primary w-100">Submit</button>

{{ form_end(form) }}

<!-- Friendly Reminder -->
<div class="alert alert-info mt-3 text-center" role="alert">
    <i class="fas fa-info-circle"></i> Please ensure your content is respectful and appropriate for all audiences. 🚀
</div>

<!-- Pop-up Modal for Validation -->
<div class="modal fade" id="validationModal" tabindex="-1" aria-labelledby="validationModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="validationModalLabel">Oops! 🚫</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Please fill in all required fields before submitting the form.
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">OK</button>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function() {
        var form = document.getElementById('clubForm');

        form.addEventListener('submit', function(event) {
            if (!form.checkValidity()) {
                event.preventDefault();
                event.stopPropagation();

                // Initialize and show Bootstrap Modal
                var validationModal = new bootstrap.Modal(document.getElementById('validationModal'));
                validationModal.show();
            }

            form.classList.add('was-validated');
        });
    });
</script>
