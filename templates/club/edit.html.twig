{% extends 'base.html.twig' %}

{% block title %}Créer un Nouveau Club{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-lg p-4">
        <h1 class="text-center text-primary mb-4">create club</h1>

        <div class="mb-4">
            {{ include('club/_form.html.twig') }}
        </div>

        <div class="text-center">
            <a href="{{ path('app_club_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> return to list
            </a>
        </div>
    </div>
</div>
{% endblock %}
