<form method="post" action="{{ path('app_choix_sondage_delete', {'id': choix_sondage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ choix_sondage.id) }}">
    <button class="btn">Delete</button>
</form>
